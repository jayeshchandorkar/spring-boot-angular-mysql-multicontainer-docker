'use strict';

describe('myApp.gameCenter module', function() {

  beforeEach(module('myApp.gameCenter'));

  describe('GameCenter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gameCenterCtrl = $controller('GameCenterCtrl');
      expect(gameCenterCtrl).toBeDefined();
    }));

  });
});